{
  "name": "backend",
  "version": "1.0.0",
  "scripts": {
    "compile": "node compile.js",
    "compile2": "solcjs -p --abi --bin --base-path . Greeter.sol -o generated/",
    "deploy": "echo \"Deploying smart contracts is not working yet, moving on...\"",
    "deploy2": "hardhat run scripts/deploy.ts",
    "deploy3": "node deploy.js",
    "deploy4": "hardhat deploy-zksync --script deploy.ts",
    "start-etn": "node --openssl-legacy-provider index.js",
    "start-etn2": "node launch2.js",
    "example": "node example.js"
  },
  "dependencies": {
    "hardhat": "^2.22.9",
    "solc": "^0.5.17"
  }
}
